{% set name = "incqueryserver-jupyter" %}
{% set version = "0.19.0.dev2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/incqueryserver-jupyter-{{ version }}.tar.gz
  sha256: b20bbe3e19393db36e467a468fc9ee7ae958887c5adf8fbfc52485abdd5756fb

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - incqueryserver-api-python-client =={{ version }}
    - ipywidgets
    - mms-python-client
    - python >=3.6

test:
  imports:
    - iqs_jupyter
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/IncQueryLabs/incquery-server-jupyter
  license: Apache-2.0
  license_file: {{ environ["RECIPE_DIR"] }}/LICENSE
  summary: IncQuery Server Client Extensions for Jupyter and Python

extra:
  recipe-maintainers:
    - FuzesiMate
    - pappist
    - abelhegedus
    - ujhelyiz
    - bergmanngabor
